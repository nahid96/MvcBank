@model MvcBank.Models.User

@{
    ViewBag.Title = "Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Hello: @Model.CardNumber</h2>

@*
    <p>Your Account Balance is @Session["Balance"]</p>
    <hr />

    @using (Html.BeginForm("WithdrawPost", "Account"))
    {
        <pre>To cash out please enter balance, card no. and pin no.</pre>

        <div class="form-group">
            @Html.LabelFor(b => b.Balance)
            @Html.TextBoxFor(b => b.Balance, new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(b => b.CardNumber)
            @Html.TextBoxFor(b => b.CardNumber, new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(b => b.Pin)
            @Html.TextBoxFor(b => b.Pin, new { @class = "form-control" })
        </div>
        @Html.HiddenFor(b => b.Id)

        <button type="submit" class="btn btn-primary">Cash Out</button>
    }
*@

<div class="col-lg-12" style="padding-top: 20px; padding-bottom: 20px;  text-align: center">
    @Html.ActionLink("Check Balance", "CheckBalance", "Bank", null, new { @class = "btn btn-primary" })
</div>

<div class="col-lg-12" style="padding-top: 20px; padding-bottom: 20px;  text-align: center">
    @*@Url.Action("Account", "WithdrawPost", new { id = UrlParameter.Optional })
        <a href="@Url.Action("WithdrawPost", "Account", new {n =1})">WithdrawPost</a>*@
    @Html.ActionLink("Withdraw", "Withdraw", "Bank", null, new { @class = "btn btn-primary" })
</div>

<div class="col-lg-12" style="padding-top: 20px; padding-bottom: 20px;  text-align: center">
    @*@Html.ActionLink("Transaction History", "TransactionHistory", "Account", new { n = 1 }, null)
        <a href="@Url.Action("", "History", new {n =1})">WithdrawPost</a>*@
    @Html.ActionLink("Transaction History", "TransactionDetails", "Bank", null, new { @class = "btn btn-primary" })
</div>

<hr />

<div class="col-lg-12" style="text-align: center">
    <pre>To exit please click on Log out button</pre>
</div>


<div class="col-lg-12" style="padding-top: 20px; padding-bottom: 20px;  text-align: right">
    @Html.ActionLink("Log out", "Logout", "Bank", null, new { @class = "btn btn-primary" })
</div>

@*check account balance, cash withdrawal and exit*@